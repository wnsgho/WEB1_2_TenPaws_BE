version: "3.7"

volumes:
  mysql_data:

services:
  tenpaws-server:
    image: 761018890747.dkr.ecr.ap-northeast-2.amazonaws.com/instagram-server:latest
    ports:
      - 8080:8080
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://tenpaws-db:3306/tenpaws
      SPRING_DATASOURCE_USERNAME: MYSQL_USERNAME_PLACEHOLDER
      SPRING_DATASOURCE_PASSWORD: MYSQL_PASSWORD_PLACEHOLDER
    depends_on:
      - tenpaws-db
    entrypoint: >
      /bin/sh -c "  
        while ! nc -z tenpaws-db 3306; do  
          echo 'Waiting for MySQL Database to be available...'  
          sleep 3  
        done  
        java -jar project.jar  
      "
    logging:
      options:
        max-size: "10m"
        max-file: "3"
        compress: "true"

  tenpaws-db:
    image: mysql
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: MYSQL_PASSWORD_PLACEHOLDER
      MYSQL_DATABASE: tenpaws
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      retries: 10
    logging:
      options:
        max-size: "10m"
        max-file: "3"
        compress: "true"